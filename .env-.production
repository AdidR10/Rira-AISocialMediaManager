# Authentication Secret (any random string - generate a secure one for production)
BETTER_AUTH_SECRET=your-random-secret-string-here

# Application URL (your droplet IP or domain)
BETTER_AUTH_URL=http://your-server-ip-or-domain

# Database Configuration (SQLite database file - Docker path)
DATABASE_URL=file:/app/data/rira.db

# Puppeteer Chrome executable path (Docker uses system Chromium)
PUPPETEER_EXEC_PATH=/usr/bin/chromium

# Google API Key (required for Gemini AI features)
GOOGLE_API_KEY=your-google-api-key-here

# Optional: Anthropic API Key (if using Claude models)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Port and Node Environment
PORT=3000
# Don't set NODE_ENV in .env file - causes Vite build issues
# NODE_ENV=production

# Optional Caddy server configuration
# echo ":80{reverse_proxy localhost:3000}" | sudo tee /etc/caddy/Caddyfile
